- name: Check files exist (warning only)
  run: |
    for file in index.html style.css script.js; do
      if [ -f $file ]; then
        echo "✅ $file found!"
      else
        echo "⚠️ $file not found (ignore if in subfolder)"
      fi
    done
